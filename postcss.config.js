const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    extend: {
      fontFamily: {
        "font-amarante": "var(--font-amarante)",
      },
    },
  },
};

export default config;